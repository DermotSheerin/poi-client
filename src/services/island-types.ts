export interface Island {
  // region: string;
  region:string;
  name: string,
  description: string,
  latitude: number,
  longitude: number,
}

export interface Region {
  region: string;
}

// Put REGION in its own class??
