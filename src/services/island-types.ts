export interface Island {
  //region:string;
  name: string,
  description: string,
  latitude: number,
  longitude: number,
}

export interface User {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
}

// export interface Region {
//   region: string;
// }

// Put REGION in its own class??
